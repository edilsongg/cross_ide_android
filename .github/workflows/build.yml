name: "Build & Release"

on:
  workflow_dispatch:

permissions:
  contents: read         # leitura de código e metadados
  actions: write         # necessário para upload/download de artifacts
  packages: write        # opcional, se usar packages

jobs:
  build:
    name: Build & Release
    runs-on: ubuntu-latest
    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Extract version from pubspec.yaml
        id: extract_version
        run: |
          version=$(grep '^version: ' pubspec.yaml | cut -d ' ' -f 2 | tr -d '\r')
          echo "VERSION=$version" >> $GITHUB_ENV

      - name: Set Up Java
        uses: actions/setup-java@v3
        with:
          distribution: oracle
          java-version: 17

      - name: Set Up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.3'
          channel: stable

      - name: Install Dependencies
        run: flutter pub get

      - name: Decode Keystore
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | tr -d '\r' | base64 --decode > android/app/keystore.jks

      - name: Create key.properties
        run: |
          cat <<EOF > android/key.properties
          storePassword=${{ secrets.KEYSTORE_PASSWORD }}
          keyPassword=${{ secrets.KEY_PASSWORD }}
          keyAlias=${{ secrets.KEY_ALIAS }}
          storeFile=keystore.jks
          EOF

      - name: Build APKs
        run: |
          flutter build apk \
            --release \
            --no-tree-shake-icons \
            --split-per-abi \
            --dart-define=VERSION=${{ env.VERSION }}

      - name: Modify Tag
        id: modify_tag
        run: |
          new_version="${{ env.VERSION }}-build-${{ github.run_number }}"
          echo "VERSION=$new_version" >> $GITHUB_ENV

      - name: Rename Artifacts
        run: |
          cd build/app/outputs/apk/release
          for file in app-*-release.apk; do
            abi=$(echo "${file}" | cut -d '-' -f 2)
            mv "${file}" "FlutterIDEAndroid_${{ env.VERSION }}_Android_${abi}.apk"
          done

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Releases
          path: build/app/outputs/apk/release/*.apk
          retention-days: 30

      - name: Create GitHub Release
        uses: ncipollo/release-action@v1
        with:
          tag: v${{ env.VERSION }}
          artifacts: build/app/outputs/apk/release/*.apk
          token: ${{ secrets.TOKEN }}
