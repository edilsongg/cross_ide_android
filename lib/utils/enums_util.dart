// ignore_for_file: constant_identifier_names

enum ThemeOption {
  //system,
  light,
  dark,
  materialYou,
  darkAmoled,
  // custom,
}

enum CompilePlatform { Windows, Linux, Mac, Web, Ios, Android }

enum CompileMode { Debug, Profile, Release }

enum CompileArch { Arm, Arm64, x86, x64 }

enum OnboardingStep { installApps, storage, sdkSelection }
